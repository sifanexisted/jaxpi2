# Reference data

Place the DNS reference dataset here as `kolmogorov_flow_Re1e6.npy` (a pickled
dict with keys `velocity`, `vorticity`, `t`, `coords`, `nu`), or point
`config.data_path` at its location. The file is not tracked in git due to its
size.
